[{{ client }}]
        key = {{ secret }}
        caps mon = allow profile rbd
        caps osd = allow profile rbd pool={{ prefix }}volumes, profile rbd pool={{ prefix }}vms, profile rbd pool={{ prefix }}images
